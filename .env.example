# ===== CONFIGURACIÓN DE PRODUCCIÓN =====
# Copia este archivo como .env.local y completa los valores

# ----- Google Sheets (Opcional) -----
# ID del Google Sheet publicado (entre /d/ y /edit en la URL)
GOOGLE_SHEET_ID=

# ----- Formulario de Contacto -----
# Servicio de email (opciones: sendgrid, resend, smtp)
EMAIL_SERVICE=resend

# API Key de Resend (https://resend.com)
RESEND_API_KEY=re_xxxxxxxxxxxx

# O si usas SendGrid
# SENDGRID_API_KEY=SG.xxxxxxxxxxxx

# Email de destino para mensajes del formulario
CONTACT_EMAIL=nandrade.socias@gmail.com

# Email remitente (debe estar verificado en tu servicio)
FROM_EMAIL=contacto@tudominio.com

# ----- Admin Panel -----
# Usuario del panel de administración
ADMIN_USERNAME=nicolas

# Contraseña del panel (usar hash en producción)
# Generar hash: node -e "console.log(require('crypto').createHash('sha256').update('tu_password').digest('hex'))"
ADMIN_PASSWORD_HASH=

# Secreto para JWT (generar con: openssl rand -hex 32)
JWT_SECRET=

# ----- Analytics (Opcional) -----
# ID de Google Analytics 4 (formato: G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=

# O Plausible Analytics
# PLAUSIBLE_DOMAIN=tudominio.com

# ----- Configuración General -----
# URL base del sitio (sin slash final)
SITE_URL=https://tudominio.com

# Ambiente: development | production
NODE_ENV=production
